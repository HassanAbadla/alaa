// middleware/buildCheck.js
export default function ({ route, redirect }) {
  // const isZain = process.env.isZain
  // const disabledPaths = ["/", "/pmis", "/pmis/", "/oss", "/oss/"]
  // // For Zain build, redirect '/' or '/pmis' to '/grc'
  // if (isZain && disabledPaths.includes(route.path)) {
  //   return redirect("/grc")
  // }
}
