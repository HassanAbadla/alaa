export default function ({ store, redirect }) {
  // check if this is GRc and send him to grc contracts
  // console.log("User type", store.$auth.user.type)
  // if (store.$auth.user.type) {
  //   return redirect(store.$auth.user.type.code == "tasks" ? "/otm/tasks" : "/otm/tasks")
  // }
  // console.log("isGRC", store.$auth.user.tenant.is_grc)
  // if (store.$auth.user.tenant.is_grc) {
  //   return redirect("/grc/contracts") //to GRC
  // }
}
